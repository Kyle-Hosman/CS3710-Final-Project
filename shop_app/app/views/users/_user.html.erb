<div class="container mt-4">
  <div class="row">
  <div class="card mb-4" role="region" aria-labelledby="user-<%= user.id %>-details">
    <div id="<%= dom_id user %>">
      <p id="user-<%= user.id %>-details">
        <strong>User name:</strong>
        <%= user.user_name %>
      </p>

      <p>
        <strong>Email:</strong>
        <%= user.email %>
      </p>

    </div>
    </div>
    </div>
</div>
